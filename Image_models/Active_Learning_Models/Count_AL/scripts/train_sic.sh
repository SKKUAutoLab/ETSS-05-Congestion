# click a gree box only if it's wrong (marked as the object but isn't) -> flips to negative
# click a red box only if it's wrong (is actually the object but marked not-object) -> flips to positive
python count_repetitive_objects.py CellSml Inbar
python count_repetitive_objects.py Logs Jonathan
python count_repetitive_objects.py Water Michael
